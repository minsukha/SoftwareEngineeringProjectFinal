<% var title = 'Roster' %>
<% var name = "" %>
<% if(!user) { %>
<% include header %>
<% } else { %>
<% var privilege = user.userInfo.privilege %>
<% name = user.userInfo.firstName + " " + user.userInfo.lastName %>
<% include header %>
<% } %>
<h1>Roster Page</h1>
<table>
<% for(var i=0; i < nameArray.length; i++) { %>
   <tr>
     <td><%= jerseyArray[i] %></td>
     <td><%= nameArray[i] %></td>
     <td><%= positionArray[i] %></td>
     <td><div class="container">
                        <!-- Trigger the modal with a button -->
                        <a href="#" data-toggle="modal" data-target="#myModal">Edit</a>

                        <!-- Modal -->
                        <div class="modal fade" id="myModal" role="dialog">
                        <div class="modal-dialog modal-sm">
    
                        <!-- Modal content-->
                        <div class="modal-content">
                        <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal">&times;</button>
                        <h4 class="modal-title">Edit Line Assignment</h4>
                        </div>
                        <form action="/updateLine" method="post">
                        <div class="form-group">
                        	<label for="lineAssignment">Line Assignment</label>
                            <input type="text" class="form-control" id="lineAssignment" name="lineAssignment">
                        </div>
                        <div class="form-group">
                        	<label for="lineAssignment">ID</label>
                            <input type="text" class="form-control" id="idNum" name="idNum">
                        </div>
                        <div class="modal-footer">
                        <button type="submit" class="btn btn-default">Submit</button>
                        </form>
                        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        </div>
      </div>
      
    </div>
  </div></td>
   </tr>
<% } %>
</table>
</p>
</body>
</html>